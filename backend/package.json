{
  "name": "course-hub-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute course-hub-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute course-hub-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}
